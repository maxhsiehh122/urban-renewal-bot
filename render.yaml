services:
  - type: web
    name: urban-renewal-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:web_app --host 0.0.0.0 --port $PORT
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: WEBHOOK_URL
        sync: false
